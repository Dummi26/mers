a = (max int) {
    println("Max: " + max.to_string())
    for i max {
        println(i.to_string())
    }
}
a.debug()
// why does this work
a.thread(10).await()
// and this just blocks and does nothing
a.run(10)
